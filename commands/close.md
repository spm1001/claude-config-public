# Session Close

**Immediately invoke the `session-closing` skill** - no preamble, no deliberation.

The ritual:
1. Gather context (git, beads, reflections)
2. Ask reflection questions
3. Execute (commit, push, update beads, write handoff)
4. Generate handoff for next session

After: remind user to type `/exit`.
